Friends in this circle:<br/><br/>
	<% users = @c.get_users(session[:user_id]) %>
	<% if users.length > 0 %>
	<ul>
		<% for user in users %>
			<li>
				<%= user.name %>
			</li>
		<% end %>
	</ul>
	<% else %>
		No users in this circle yet.
	<% end %>
<br/><br/>
<input type="button" id="backbtn" value="Back" href="/circle/view_all"/>