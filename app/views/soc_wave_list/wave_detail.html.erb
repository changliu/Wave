

<div id="bgimg">
</div>
<div class="bg"></div>
<!-- <img class="bg" src="/assets/ikes.jpeg"/> -->
<div id = "moreinf">
<!-- <h2> WAVE DETAILS </h2> -->
<h2 id = "header"> </h2>
<p id="text">

</p>

<div id = "getout">
<a  class = "na" href="../soc_wave_list">Back</a>
<a  class = "na" href="wave_final">Ride this Wave</a>
</div>
</div>




<script>
  	var a=document.getElementsByTagName("a");
  	for(var i=0;i<a.length;i++){
      	a[i].onclick=function(){
          	window.location=this.getAttribute("href");
          	return false
      	}
  	}
   	var vars = [], hash;
  	var q = document.URL.split('?')[1];
  	if(q != undefined){
    	q = q.split('&');
    	for(var i = 0; i < q.length; i++){
      		hash = q[i].split('=');
      		vars.push(hash[1]);
      		vars[hash[0]] = hash[1];
    	}
	}
	// console.log(q);
	var id = vars['id'];
	var dist = vars['dist'];
	var name = vars['name'];
	var token = vars['token'];
	// console.log(token);
	console.log(name);


	$("#header").append("<h2>" + unescape(name) + "</h2>");
	// $("#text").html(unescape(name) + "<br/>" + unescape(description));

	//query to download all photos links of a venue
 	var query = "https://api.foursquare.com/v2/venues/" + id + "/photos?";
 	var query2 = "https://api.foursquare.com/v2/venues/" + id + "/tips?"   
 	    params = new Object();
        params.group = "venue"
        params.oauth_token = token;
        $.ajax({
          	url: query,
            dataType: 'json',
            data: params,
            success: fs_photo_query_success
        });


        $.ajax({
          	url: query2,
            dataType: 'json',
            data: params,
            success: fs_tips_query_success
        });

//to be implemented: wave details on tips


//to be implemented, all photos loaded and have a slide show in the background
//right now, it only shows the first photo as a static image
   function fs_photo_query_success(data) {
        var photo = data.response.photos.items[0].sizes.items[1].url;
        var bg_img = "<img class='bg' src=" + photo + " style='opacity:0.7'" + " />";
        $("#bgimg").append(bg_img);

    }

    function fs_tips_query_success(data) {
    	console.log(data);
    }


 </script>